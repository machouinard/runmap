# Local env for scripts and web viewer (do NOT commit real secrets)
# Copy to .env and fill in values

# Supabase project
SUPABASE_URL=https://ppuxdqqxhxfayjbsfzsw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY   # scripts only (server-side)
SUPABASE_ANON_KEY=REPLACE_WITH_ANON_KEY                    # web viewer/client

# Postgres
# For short queries (default), use the transactional pooler:
PGHOST=aws-1-us-west-1.pooler.supabase.com
PGPORT=6543
# For long-running maintenance (refresh/materialize), use DIRECT connection (bypasses pooler):
# Find this in Supabase -> Project Settings -> Database -> Connection string (non-pooled)
# Example (replace with your project's values):
# PGLONG_HOST=db.ppuxdqqxhxfayjbsfzsw.supabase.co
# PGLONG_PORT=5432
# PGLONG_USER=postgres
# PGLONG_DB=postgres
PGDATABASE=postgres
PGUSER=postgres.ppuxdqqxhxfayjbsfzsw
PGPASSWORD=REPLACE_WITH_DB_PASSWORD

# PMTiles export destination (object key under 'tiles' bucket)
DEST=streets_unrun.pmtiles

# Web viewer configuration (Vite reads VITE_*)
VITE_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
VITE_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
# Public URLs to your PMTiles objects (after you upload with export_tiles.sh)
# Unrun (required)
VITE_PM_TILES_URL=${SUPABASE_URL}/storage/v1/object/public/tiles/${DEST_UNRUN}
# Optional overlays (runs lines, coverage buffer)
# VITE_PM_TILES_URL_RUNS=${SUPABASE_URL}/storage/v1/object/public/tiles/${DEST_RUNS}
# VITE_PM_TILES_URL_BUFFER=${SUPABASE_URL}/storage/v1/object/public/tiles/${DEST_BUFFER}

# Styling overrides (hex colors)
# VITE_UNRUN=#e53935
# VITE_RUNS=#1e88e5
# VITE_BUFFER=#7e57c2
